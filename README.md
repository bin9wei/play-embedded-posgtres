# play-embedded-posgtres

## How to launch PG locally

    # cd to directory:
    $ cd localPG

    # Bring up virtual box
    $ vagrant up

    # ssh to virtual box
    $ vagrant ssh

    # Shut down virtual box
    $ vagrant halt